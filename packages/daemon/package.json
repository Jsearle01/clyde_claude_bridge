{
  "name": "@claude-bridge/daemon",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "main": "./dist/main.js",
  "types": "./dist/main.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run",
    "lint": "eslint src tests"
  },
  "dependencies": {
    "@claude-bridge/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0"
  }
}
